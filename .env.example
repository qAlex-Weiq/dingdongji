# ============================================================
# 叮咚机 · 环境变量配置模板
# 使用方法：复制本文件为 .env，填入对应 Key 后重启服务即可
# （.env 已被 .gitignore 忽略，不会提交到仓库）
# ============================================================

# ---------- 景点模块数据源（三选一，均可不配） ----------
# 不配置任何 Key 时，景点模块自动使用内置演示数据（离线可用，覆盖 25 个城市）。
#
# —— 方案 A（推荐）：高德地图 Web 服务 API ——
# 真实联网 POI 数据（含评分、人均消费、照片、开放时间），个人开发者免费额度充足。
# 1. 注册并实名认证：https://console.amap.com
# 2. 「应用管理」→ 创建新应用 → 添加 Key，服务平台务必选「Web服务」
# 3. 将 Key 填入下面并取消注释
# AMAP_KEY=你的高德Web服务Key
#
# —— 方案 B：LLM Agent 生成 ——
# 兼容 OpenAI Chat Completions 协议的任意接口；默认对接智谱 GLM（glm-4-flash 免费）。
# 1. 注册：https://open.bigmodel.cn → 创建 API Key
# 2. 填入下面并取消注释
# LLM_API_KEY=你的APIKey
# LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# LLM_MODEL=glm-4-flash
# LLM_WEB_SEARCH=1        # 设为 1 时模型先联网检索再生成（更准但更慢，仅智谱等支持）
#
# —— 强制指定数据源（amap | llm | local）——
# 默认按「高德 → LLM → 内置数据」顺序自动选择第一个可用的。
# SIGHT_SOURCE=amap

# ---------- 服务端口 ----------
# PORT=3000
