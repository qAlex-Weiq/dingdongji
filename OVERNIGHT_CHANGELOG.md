# 夜间自动开发记录（Overnight Changelog）

> 执行时间：2026-09-12 · 模式：overnight autonomous
> 基线：`d3a6abd` → 产出 5 个提交，**未改动队友任何 UI 文案与样式**

---

## TL;DR

| Task | 内容 | 状态 |
|------|------|------|
| 1 | 同步并保留队友 UI 改版 | ✅ |
| 2 | 美食页加入行程按钮 + 特色菜自动匹配餐厅 + 三餐标准化 | ✅ |
| 3 | 全局行程上下文（单城市 + 日期） | ✅ |
| 4 | 一键全包规划 | ✅ |
| 5 | 行程补全建议（AI 建议添加） | ✅ |

**测试：** `test-itinerary` 88 通过 / 0 失败（原 54，新增 34 条断言）·
`test-price-clamps` 40 通过 · `test-strict-tier-filter` 11 通过 · 全新克隆可跑通。

---

## 提交列表

```
2a42fba chore(sync): 同步队友 UI 改版并重新挂载行程篮钩子
aa60fd1 feat(food): 补齐加入行程按钮 + 特色菜自动匹配餐厅 + 标准化三餐时段
f7a6954 feat(plan): 行程补全建议 + /api/plan 支持 dish 类型与 autoFill
b583c5c feat(plan): 一键智能规划 + AI 建议采纳/移除交互
4d164f1 fix(build): 补提交 server/lib/district.js
```

---

## Task 1 · 保留队友 UI 改动（最先处理）

开工时发现两件事同时存在：

1. `origin/main` 上有 **2 个我本地没有的队友提交**
   （`de24ed4` 品牌升级「订懂机」+ 全站视觉改版，3189 行 CSS；
   `f81182f` LLM 进度横幅 / 准点率徽标 / 车票筛选 / 快捷示例）
2. 我本地有未提交的行程篮钩子，且**改的是同一批文件**

**处理方式**：先把本地改动 stash 并另存补丁（`/tmp/ddj-local-hooks.patch`），
`--ff-only` 快进到队友提交，再把钩子重新贴回去。5 个 HTML 文件有冲突，
逐个按「队友优先、我方只做叠加」解决：

| 冲突点 | 决定 |
|--------|------|
| 导航文案 `美食` vs 我的 `饭店` | **用队友的「美食」**，放弃我方措辞 |
| `llm-progress.js` vs `cart.js` | 两者并列引入，不二选一 |
| 品牌「订懂机 / 订」 | 全量保留，并把遗漏的 `plan.html` 一并对齐 |

**校验结果**：相对队友提交净变更 **32 行新增、0 行删除**；
`app.js`、`style.css`、`llm-progress.js` 与队友提交**逐字节一致**。

> ⚠️ 需要你确认的一点：`plan.html` 原本还停留在旧品牌「叮咚机/叮」，
> 我按队友改版统一成了「订懂机/订」。如果这是有意保留的，请回退该处。

---

## Task 2 · 美食模块行程篮打通

### 根因（原按钮从未出现）

`food.js` 里餐厅卡片写的是：

```js
${window.Cart && r.city ? Cart.addButton('food', r.city, r) : ''}
```

而 `/api/food/restaurants` 返回的餐厅对象**根本没有 `city` 字段**
（实测 keys：`id,name,cuisines,avgPrice,priceRange,location,hours,rating,…`）。
三元判断恒为假 → 永远渲染空串。改用当前查询城市 `cityInput.value` 作为归属地。

### 特色菜 → 餐厅自动解析

- 「特色菜品」卡片新增按钮，类型为 **`dish`**（想吃什么），与 `food`（去哪吃）分开。
- 新增 `resolveDishes()`：把 dish 解析成具体餐厅，打分优先级

  | 分数 | 规则 | 例 |
  |------|------|-----|
  | 3 | 招牌菜精确命中 | 全聚德 `signatureDishes: ['北京烤鸭']` |
  | 2 | 店名含菜名 | 「全聚德**烤鸭**」 |
  | 1 | 子串互含 | 「烤鸭」↔「焖炉烤鸭」 |

  同分再按 **同区优先 → 评分降序 → 名称升序**（完全确定性）。
- 菜名带地域前缀（北京烤鸭）而店家招牌常写作「烤鸭」，故同时用全名与 `aliases` 匹配。
- 已在篮中的餐厅不会被重复选中；匹配不到的菜**给 warning 而非静默丢弃**。

**实测**：`北京烤鸭` → 全聚德烤鸭（前门店），东城区，与当日景点同区，
时间轴理由显示「为「北京烤鸭」匹配 · 与当日行程同在东城区 · 营业至 21:30 · 人均 ¥220」。

### 三餐时段标准化

| 餐 | 时间 | 说明 |
|----|------|------|
| 早餐 | 09:00–09:30 | 酒店早餐优先，无酒店则「街边早点」 |
| 午餐 | 12:30 起 | 原为 12:00 |
| 晚餐 | 18:30 起 | 保持 |

早餐使用**独立的 `breakfast` 类型**而非 `food`：它是「酒店/街边」，
不应该消耗用户亲手挑的正餐餐厅，也不参与就近配餐的距离计算。
（这一点最初用 `food` 类型实现，导致 2 条既有测试失败 —— 现已修正。）

**顺带修掉一个漂移 bug**：景点时段的上/下午标签原本以午餐基准为界，
午餐改到 12:30 后，12:00–12:30 的景点会被标成「上午」。现固定按 12:00 判定。

---

## Task 3 · 全局行程上下文

- 新增 `localStorage` 键 `ddj.ctx.v1`：`{ city, travelDate }`，**与行程篮分开存**
  —— 即使用户清空篮子，仍记得这趟要去哪。
- 首次加入条目时自动写入 `currentCity`。
- 景点 / 酒店 / 美食三页：`Cart.prefillCity()` 自动预填城市，
  **预填成功才**触发一次默认搜索（用户自己填的、或没有上下文时都不触发，
  避免打断正在输入的用户）。
- 导航栏渲染 `📍 北京` 指示器（`#ddj-city-indicator`，复用 `--primary-soft`）。
- **切换城市守卫** `guardCitySwitch()`：篮中有其他城市条目时弹确认，
  文案按需求原文：

  > 当前行程篮中已有【北京】的项目，切换城市将清空行程篮重新开始，是否继续？

  用户点取消 → **输入框回滚到原城市**，篮子分毫不动。

---

## Task 4 · 一键全包规划

`plan.html` 顶部新增「🚀 一键智能规划」卡片：
出发城市（选填）/ 目的地（自动预填）/ 出发日期 / 天数（1–5，默认 3）/
三档药丸（🏷️ 经济实惠 · ☕ 舒适均衡「推荐」· ✨ 品质轻奢）。

**架构：按要求走客户端批量写入，没有建任何服务端文件缓存。**

点击后并行拉取四个模块，按档位与天数选条目：

| 类型 | 数量 | 规则 |
|------|------|------|
| 景点 | `days × 2` | 热度序，首个标必去 |
| 酒店 | 1 | 按档位 `budget/comfort/luxury` |
| 餐厅 | 2–3 | 按档位人均区间 |
| 特色菜 | 2 | 交给服务端解析成同区餐厅 |
| 车票 | 0–1 | **仅填了出发城市时**查询 |

车票策略：经济 = 最低价（价格取自 `seats[].price`），舒适/轻奢 = 上午 06:00–12:00 车次。
**车票查询失败静默跳过**，不能因为查不到票就让整个一键规划失败。

条目经 `Cart.addMany()` 一次性写入（replace 语义），只写一次 storage、只通知一次订阅者，
不会对每个条目重复弹城市确认框。

**Universal Sync 自然成立**：数据都在 localStorage，用户跳到景点/美食页即显示
「✓ 已加入」，可随时增删后回来重新生成。

---

## Task 5 · 行程补全建议

- 服务端 `suggestSights()`：景点数 `< days × 2` 时补充高分推荐，
  排序为 **同区优先 → 热度 → 名称**（确定性）。
- 用户已选景点**原样保留**，必去仍锚定 Day 1；补充项标 `suggested: true`。
- 前端渲染 `💡 AI 建议添加` 徽标 + `✓ 采纳` / `✕ 移除`，
  每天标题栏有克制的文字链「移除所有 AI 建议」。
- 采纳 → 写回行程篮；移除 → 记入 `state.dismissed`，**重新生成时保持移除**。
- `/api/plan` 支持 `autoFill: false` 彻底关闭补全。

**顺带放宽了一个门槛**：`/api/plan` 原本要求至少 2 个景点，
既然不足部分服务端会自动补全，就没理由再把用户卡在表单前 —— 改为至少 1 个锚点。

---

## 自主决策记录（供 review）

按「最简单、最少摩擦的标准 UX」原则做的判断：

1. **`dish` 作为独立类型**而非复用 `food` —— 「想吃烤鸭」和「去全聚德」是两件事，
   前者需要服务端按当天行政区解析，后者是用户已经定好的。
2. **早餐用 `breakfast` 类型** —— 理由同上，且避免消耗正餐餐厅池（有测试守着）。
3. **一键规划取 `days × 2` 个景点** —— 与补全阈值对齐，
   保证一键生成的行程不会立刻又触发一堆 AI 建议。
4. **车票失败不阻断** —— 12306 是外部实时依赖，不该成为单点故障。
5. **`prefillCity` 只在真的预填了才触发搜索** —— 否则会打断手动输入的用户。
6. **移除的建议记在内存而非 localStorage** —— dismiss 是「这次不要」，
   不该跨会话污染用户的篮子。
7. **一键规划用 replace 语义** —— 「一键全包」在用户心智里是重新开始。

---

## 测试与验证

```
node scripts/test-itinerary.js       → 88 通过 / 0 失败（新增 34 条）
node scripts/test-price-clamps.js    → 40 通过 / 0 失败
node scripts/test-strict-tier-filter.js → 11 通过 / 0 失败
```

新增测试小节：
- **10. 特色菜自动匹配餐厅** —— 打分规则、解析、去重、无匹配兜底、端到端
- **11. 三餐时段** —— 基准值、早餐类型与落点、午/晚餐不早于基准
- **12. 行程补全建议** —— 触发阈值、标记、不重复、确定性、`autoFill=false`

**浏览器实测**（Playwright，本地 3000）：
- 一键规划「北京 / 3 天 / 舒适均衡」→ 批量写入 12 项（6 景 1 酒 3 餐 2 菜），
  生成 3 天行程，`📍 北京` 与 ctx 同步
- 稀疏场景「4 天 / 2 景点」→ 6 条建议、6 组采纳/移除按钮、3 个「移除全部」链接
- 时间轴实测三餐：早餐 09:00 酒店早餐、午餐 13:30、晚餐 18:30 全聚德
- 控制台 **0 error**

**全新克隆验证**：`git clone` + `npm install` + 跑测试 → 88 通过。
（过程中发现 `server/lib/district.js` 被 `itinerary.js` require 却一直没纳入版本控制，
全新克隆会 MODULE_NOT_FOUND —— 已补提交。）

---

## 已知事项 / 未做的事

- **`scripts/smoke-test.js` 跑不完**：它会打 `source=auto` 的接口，
  该路径走真实 LLM，实测单次 >20s 直接挂住。
  **与本次改动无关** —— 已单独验证本地数据源接口全部 200
  （cities 25 城 / sight 28 / hotel 10 / food 7 / specialties 5，
  12306 车票 339ms 正常）。建议后续给它加超时或 `--offline` 开关。
- **严格按 Task 1–5 收口，没有夹带任何额外功能。**
  唯二的越界是两个「不修就是坏的」：`plan.html` 品牌对齐、
  `district.js` 补提交 —— 都已在上文标出。
- 根目录散落十几张 unsplash 图片和 `preview-*.png` 仍未跟踪，
  不清楚是否要入库，**没动它们**。
- 界面新元素全部复用既有 CSS 变量与 `.chip` / `.field` 语言，
  未硬编码任何颜色；实测自动继承了队友新配色（绿 / 米）。
